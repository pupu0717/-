platform :ios, '14.0'

# CocoaPods analytics
install! 'cocoapods', :integrate_targets => false

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  # Flutter dependencies
  flutter_application_path = '../..'
  load File.join(flutter_application_path, '.ios', 'Flutter', 'pod_helper.rb')

  # Your other pods here
end
